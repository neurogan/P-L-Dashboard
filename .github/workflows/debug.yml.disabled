name: Debug Agent
on: [push, pull_request]

jobs:
  debug:
    uses: obrandrup/debug-agent-central/.github/workflows/debug-agent-reusable.yml@main
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
